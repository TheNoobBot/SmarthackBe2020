server:
  servlet:
    context-path: /smarthack-be/api/
  port:
    8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smarthack
  jpa:
    hibernate:
      ddl-auto: update # change this to none after db is created
